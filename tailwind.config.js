const { colors } = require("./src/cwo-designtokens")

module.exports = {
  theme: {
    colors: colors,
    extend: {
      fontFamily: {
        display: ["DaysLater", "Helvetica", "sans-serif"],
      },
    },
  },
  variants: {},
  plugins: [],
}
